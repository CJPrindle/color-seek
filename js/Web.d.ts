export declare namespace Web {
    /**
     * @name Http
     * @description Provides access to HTML and/or CSS files to parse for
     *              color values
     * */
    class Http {
        parseUrl(url: string, callback: any): Promise<void>;
    }
}
